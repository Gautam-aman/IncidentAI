apiVersion: v1
kind: ConfigMap

metadata:
  name: auth-config
  namespace: incidentai

data:

  SPRING_DATASOURCE_URL: >
    jdbc:postgresql://postgres:5432/incident_auth_db

  SPRING_REDIS_HOST: redis